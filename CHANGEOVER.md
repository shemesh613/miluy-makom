# החלפת מערכת שעות בתחילת שנה — מדריך

מה עושים כשמשה שטיינר שולח את "מערכת תשפ״X" במייל.

---

## הדרך המהירה (רוב השנים)

⚙️ **הגדרות** → **עדכון קובץ מערכת** → העלה את `מערכת תשפX למלמדים.xlsx` → **אשר והחלף מערכת**.

הקובץ נקרא בדפדפן, נשמר ב-Firebase תחת `scheduleOverride`, ומסתנכרן מיידית לכל
המכשירים. אפשר תמיד לחזור למערכת המובנית בכפתור "↩️ חזרה למערכת המובנית".

**מה ההעלאה מעדכנת:** שעות ההוראה של כל מלמד, איזו כיתה בכל שעה, ורשימת המחנכים.

**מה היא *לא* מעדכנת — ולכן צריך לבדוק כל שנה:**

| דבר | איפה בקוד | מתי זה נשבר |
|---|---|---|
| `parallelClasses` / `classesWithParallel` | ראש ה-`<script>` | כשכיתה נפצלת או מתאחדת |
| `teacherContacts` | ראש ה-`<script>` | מלמד חדש / מישהו עזב |
| `YARD_DUTY` / `YARD_SUBS` | מודול תורנות החצר | כל שנה — התורנות נבנית מחדש |

בתשפ״ז כל השלושה השתנו, ולכן הוטמעה המערכת ישירות בקוד במקום להסתמך על ההעלאה.

---

## הדרך המלאה (כשהמבנה משתנה)

### 1. הקבצים

| מה | מאיפה |
|---|---|
| `מערכת תשפX למלמדים.xlsx` | מייל ממשה שטיינר, נושא "מערכת תשפ״X". שני גיליונות: `לפי כתות`, `לפי מלמדים` |
| אילוצי המלמדים | טופס גוגל "אילוצי מערכת תשפ״X" + מיילים חופשיים לפני תחילת השנה |
| התורנות | נבנית מהשניים האלה — ראו `תדריך תורנות חצר תשפז.md` בשולחן העבודה |

### 2. פענוח הקובץ

`parse_sched.py` מחקה בדיוק את הפרסר של האפליקציה (`parseScheduleRows`), עם שני
תיקונים שהפרסר בדפדפן לא עושה:

- **שעה "4.5"** — בדפדפן הופכת לשעה מזויפת בשם `'4.5'`; בפייתון היא מתמזגת לשעה 4.
- **מחנך בלי שם כיתה בתא** — כשכתוב רק `ב"מ` / `ברכות` / `מחנך`, משלימים את כיתתו
  שלו. בלי זה מחנכי ז׳ ו-ח׳ יוצאים בלי אף כיתה.

### 3. בדיקות שחייבות לעבור

- מספר המלמדים ומספר המשבצות השבועיות הגיוניים (תשפ״ז: 29 מלמדים, 480 משבצות)
- כל מחנך משויך לכיתה, כולל מחנכי ז׳/ח׳
- `parallelClasses` תואם למבנה הכיתות בפועל
- אין שם עם רווח כפול (`הרב  אליהו שמשון`) — מנרמלים, אחרת השם לא מתאים בין
  המערכת, התורנות ופרטי הקשר
- אין מלמד ברשימה בלי פרטי קשר (מופיע כאזהרה בהגדרות)

### 4. תורנות החצר

`gen_subs_full.py` בונה לכל תורנות רשימת מחליפים מדורגת + לאילו תורנויות הנעדר
יכול להחזיר. הוא **מאמת קודם שהשיבוץ שהוא מחשב זהה לזה שפורסם** — אם לא, הוא
נעצר. זה השומר מפני פרסום לוח אחד ועבודה לפי לוח אחר.

### 5. הטמעה ופריסה

`patch_app.py` מחליף את כל הבלוקים ב-`index.html` ומזריק את מודול התורנות.
אחר כך: `node --check` על הסקריפט, בדיקת דפדפן (`test_app.js`), ופריסה ל-`master`
(GitHub Pages מגיש משם).

⚠️ **חשוב:** אחרי הטמעה בקוד, עדכן את `BUILTIN_SCHEDULE_STAMP` לתאריך של היום.
`override` ישן שהועלה בשנה קודמת נזרק אוטומטית לפיו — בלי זה הוא ידרוס את
המערכת החדשה בכל טעינה.

---

## איך עובד הסנכרון עם תורנות החצר

מלמד שדווח כנעדר בשעה **צמודה להפסקה** (למשל בוקר/4 עבור 10:05) — התורנות שלו
באותה הפסקה עוברת אוטומטית:

1. **מחליף פנוי** — הראשון ברשימת המועמדים שאינו נעדר ואינו כבר בתורנות באותה
   הפסקה. הנעדר מחזיר לו תורנות שלו בהמשך השבוע (`repay`).
2. **מועמד הבא** — אם הראשון נעדר גם הוא.
3. **ממלא המקום** — אם כל המועמדים תפוסים, מי שממלא את מקומו בשיעור לוקח גם את
   התורנות.

רשימות המועמדים נבנו לפי אותם כללי ברזל של בניית התורנות: רק בתוך שעות הנוכחות
של המלמד, בהתאמה לקבוצת הגיל שיוצאת להפסקה, ולפי האילוצים האישיים.

אפשר לעקוף ידנית בכפתור "🔄 החלף"; הבחירה נשמרת ב-Firebase תחת `yardSwaps`
ומסתנכרנת לכולם. "↺ אוטומטי" מחזיר לבחירה האוטומטית.
